To each of the below questions - generate a two line answer in points:

1. Finding structure in performance metrics : Considering we need to decrease the performance metrics like iq1, stability and output_voltage_difference, can you identify the pair of all metrics that show tradeoffs - for example tradeoff(iq1, output_voltage_difference) represents a tradeoff between quiscent current iq1 and output_voltage difference which is output voltage differenced with the reference voltage.

2. Finding structure that connects circuit sub-structures to performance metrics : Considering the ** Circuit Netlist ** given above, what are the critical substructures for the low dropout regulator to optimize performance metrics like iq1, stability and output_voltage_difference ? Furthermore, what are the critical parameters associated with these substructures and what performance metrics does it affect - for example create a tuple like (error differential pair substructure, bias transistors , has critical parameters l1,w1,l2,w2 , which affects output_voltage_difference through error amplifier )

3. Detailing how to optimize individual performance metrics : For each performance metric provide a one line description of how to tune the associated critical parameters of the associated sub-circuit structures by making use of the tuple structure of point 2. For example - For the output_voltage_difference to be low critical paramaeters of the differential stage need to be tuned in particular manner for high or low amplification.

4. Detailing how to optimize parameters considering tradeoffs : Now, for each tradeoff(P1, P2) pair identified in point 1, enumerate in a single line as to how parameters need to be tuned to jointly optimize tradeoff(P1, P2) - for example for high value of P1, parameters (l1, l2) need to increased, while for high values of P2, parameters (w1, w2, w12) need to increased. 